package br.com.believeon.projeto.service;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

import br.com.generation.sistemafaculdade.dao.ProjetoRepo;
import br.com.generation.sistemafaculdade.model.Projeto;



@Component
public class ProjetoService implements IProjetoService {

	@Autowired //declarando a interface do DAO que eu criei
	private ProjetoRepo repo; 
	
	@Override
	public List<Projeto> recuperarTodos(){
		
		return (List<Projeto>) repo.findAll();
	}

	@Override
	public Projeto recuperarPorId(int idProj) {
	
		return repo.findById(idProj).get();
	}
	
	public void adicionarNovoProjeto(Projeto projeto) {
		repo.save(projeto); //save = utilizado para insert e update
						  //se a chave primaria ja existir = update
						  //se a chave primaria ainda não existir = insert
	}
}
